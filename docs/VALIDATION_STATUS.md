# VALIDATION_STATUS.md

> 기준일: 2026-05-17
> 목적: Phase 1 Runtime Governance 준비 상태 검증 기록

---

| 항목 | 상태 | 확인일 |
|------|------|--------|
| rollback_ready | ✅ PASS | 2026-05-16 |
| schema_locked | ✅ PASS | 2026-05-16 |
| adapter_ready | ✅ PASS | 2026-05-16 |
| merge_journal_ready | ✅ PASS | 2026-05-16 |
| single_account_e2e | ✅ PASS | 2026-05-17 |
| phase2_dup_upload_guard | ✅ PASS | 2026-05-17 |
| phase2_queue_recovery | ✅ PASS | 2026-05-17 |
| phase2_token_expiry | ✅ PASS | 2026-05-17 |
| phase2_multiaccount_race | ✅ PASS | 2026-05-17 |
| phase2_retry_consistency | ✅ PASS | 2026-05-17 |
| watchdog_launcher_recovery | ✅ PASS | 2026-05-17 |
| watchdog_n8n_guard | ✅ PASS | 2026-05-17 |
| watchdog_fail_counter | ✅ PASS | 2026-05-17 |
| watchdog_slack_env_fix | ✅ PASS | 2026-05-17 |
| health_check_tool | ✅ PASS | 2026-05-17 |
| **phase2_runtime_governance_ready** | ✅ **PASS** | **2026-05-17** |
| schema_orphan_cleanup_260526 | ✅ PASS | 2026-05-26 |
| persona_profile_per001_created_260526 | ✅ PASS | 2026-05-26 |
| backup_3_confirmed_260526 | ✅ PASS | 2026-05-26 |
| watchdog_flask_dual_fixed_260527 | ✅ PASS | 2026-05-27 |
| dual_scheduler_resolved_260527 | ✅ PASS | 2026-05-27 |
| virtual_autoreply_proof_260528 | ✅ PASS | 2026-05-28 |
| supplier_blocklist_fieldmap_fix_260703 | ✅ PASS | 2026-07-03 |
| watchdog_task_wrapper_260708 | 🟡 PARTIAL | 2026-07-09 |
| quality_gate_relevance_filter_canary_260706 | 🔴 FAILED → ROLLED_BACK | 2026-07-06 |
| quality_gate_relevance_filter_redesign_260707 | 🟡 PARTIAL | 2026-07-07 |
| di_canary2_airtable_integrity_260704 | ✅ PASS | 2026-07-04 |
| di_canary3_kpi_collector_260705 | ✅ PASS | 2026-07-05 |
| launcher_duplicate_instance_cleanup_260706 | 🟡 PARTIAL | 2026-07-06 |
| inc028_1st_shutdown_root_cause_confirmed | ✅ PASS | 2026-07-10 |
| heartbeat_wake_to_run_applied | 🟡 PARTIAL | 2026-07-10 |
| pending_a_session0_adspower_verified | ✅ PASS | 2026-07-10 |
| testabd_diagnostic_tasks_disabled | ✅ PASS | 2026-07-10 |
| nssm_dual_watchdog_resolved_260711 | ✅ PASS | 2026-07-11 |
| nssm_crash_restart_verified_260711 | ✅ PASS | 2026-07-11 |
| nssm_reboot_proof_260711 | ✅ PASS | 2026-07-11 |
| ngrok_localsystem_fix_260711 | ✅ PASS | 2026-07-11 |
| training_review_grid_50batch_260712 | 🟡 PARTIAL(조건부 종결) | 2026-07-12 |
| training_review_verification_error_fix_260712 | ✅ PASS | 2026-07-12 |

> ⚠️ **scope 한정:** single-account E2E + 운영 안정화 검증 완료. 다계정 실운영 evidence는 Phase 3 대상.

---

## 근거

| 항목 | 근거 |
|------|------|
| rollback_ready | `snapshots/snapshot_260516_project/`, `snapshot_260516_db/`, `snapshot_260516_.env` 생성 완료 |
| schema_locked | `docs/schema_governance.md` 작성 완료 — Migration Forbidden 규칙 명시 |
| adapter_ready | `docs/BRIDGE_SKELETON_POLICY.md` 작성 완료 — bridge contract 정책 확정 |
| merge_journal_ready | `porting_logs/MERGE_JOURNAL.md` 생성 완료 — 이식 기록 체계 수립 |
| single_account_e2e | Pillow crop + imgbb 전처리 후 `ig_media_id=18116524126780958` 생성 확인 (INC-010) |
| phase2_dup_upload_guard | `save_to_airtable()` 동일 image_url 재호출 시 "중복 이미지 - 저장 생략" 반환, 레코드 수 1 유지 확인 |
| phase2_queue_recovery | 재시작(PID 30916→34916) 후 큐 워커가 pending 태스크 픽업·처리 확인 (id=5, status=dead) |
| phase2_token_expiry | OAuthException 190 감지 → Slack 직접 호출 + Airtable failed 마킹 확인 (ERR-017 수정 후 재검증) |
| phase2_multiaccount_race | 코드 분석: race condition 2건(ERR-018) 발견 → uploading 잠금 + max_instances=1 수정 완료 |
| phase2_retry_consistency | Part A: failed→posted 기존 확인 / Part B: ig_media_id 가드로 중복 업로드 차단 확인 (ERR-019) |
| watchdog_launcher_recovery | launcher PID 27104 강제 종료 → watchdog 감지(23:04:23) → 6초 내 재시작 성공(23:04:29) / watchdog.log 직접 확인 |
| watchdog_n8n_guard | n8n 프로세스 전체 강제 종료(포트 5678 CLOSED 확인) → watchdog 감지 → 자동 재시작(23:05:34~23:06:44) / watchdog.log 직접 확인 |
| watchdog_fail_counter | `$failCount` 해시테이블 — 서비스별 연속 실패 카운터, 3회 이상 시 Slack error 알림 |
| watchdog_slack_env_fix | `.env` SLACK_WEBHOOK_URL 자동 로드 추가 — 시스템 환경변수 미설정 시 .env 폴백 (ERR-020) |
| health_check_tool | `tools/check_runtime_health.py` 실행 확인 — launcher PID 27104 / n8n PID 13724 / port 5000·5678 OPEN / Airtable HTTP 200 / posted 97건 정상 출력 |
| **phase2_runtime_governance_ready** | 단일계정 E2E + 운영 안정화(watchdog 자동복구 실증) 완료. 다계정 실운영 evidence는 별도 미보유. Phase 3 진입 가능 상태. |
| caption_blocklist_260629 | content_filter.py CAPTION_BLOCKLIST 추가(coslife, lily) — passes_keyword_filter() 선행 차단. OCR 없이 caption 텍스트 레벨 차단 적용. ERR-044 MITIGATED. |
| schema_orphan_cleanup_260526 | 孤兒 필드 4개 삭제 확인 — `caption copy`, `post_url copy`, `error_message` (Instagram_Posts), `source_url (URL)` (Source_Feeds). grep 코드 참조 0건 확인 후 Airtable UI 직접 삭제. |
| persona_profile_per001_created_260526 | Persona_Profile 테이블 신규 생성 (tblbxtUH1K88aomOP, 12개 필드) + PER-001 레코드 생성 완료. record_id: reck5gPdhpWqgmdKP, persona_name: 엔틱, account_code_ref: ACC-001 연결. |
| backup_3_confirmed_260526 | `C:\backup_(3)_260526 1350_SNS_24AutoProject_260511.zip` 백업 완료 확인. |
| virtual_autoreply_proof_260528 | Flask :5000 PID 14256 + ngrok :4040 PID 8956 LISTENING 확인. 로컬 POST 200 OK. detect_price_inquiry=True. DEFAULT_BASE_PRICE=50000 적용. Airtable LI-2B0A72F7 생성·qualified/auto_replied 확인. IG 발송 실패는 TEST_SENDER_004 가상 ID 정상 예상 결과. backup_(7)_260528_1338 완료. |

| repository_interface_260624 | AirtableRepository 22개 메서드 전면 교체 완료 — Infrastructure 외부 직접 호출 0건 확정. Failure Injection Test PASS (finally/AdsPower Stop 정상). Runtime Proof 5회 연속 정상 (19:50~21:50 KST). 커밋 체인: 18aa3a7→df9df6b→4502e65→e0bcff6→36cbf05. |
| caption_generate_260629 | generate_caption_clone → generate_caption 교체 완료. clean_fb_metadata _ui_pat 추가(원본보기·번역평가·좋아요·공유하기·저장). 해시태그 Korea-related only 프롬프트 규칙 추가. 커밋 998215e. |
| supplier_blocklist_fieldmap_fix_260703 | ERR-046/FP-034/INC-024 해결. `repository_interface.py`/`airtable_repository.py`/`facebook_crawler.py` 3파일 `supplier_name`→`author_name`+`page_name` 매핑 수정. Gate 6 ISOLATED INTEGRATION PROOF(격리 테스트 테이블 `Supplier_Blocklist_Test`, 실 HTTP 왕복) 사전 통과 + 운영 `Supplier_Blocklist` 5건 대상 Runtime Proof 6/6 매칭 성공. pytest 100 passed, pre-existing 4 failed는 stash 비교로 무관 확인. |
| di_canary2_airtable_integrity_260704 | Canary #2 — modules/metrics/airtable_integrity.py의 airtable_bridge.get_table() 직접 호출을 AirtableRepository.fetch_posted_missing_media_id()로 치환. repository_interface.py(ABC 계약 추가) + airtable_repository.py(구현, AND({post_status}='posted', {ig_media_id}='') 필터) + tests/test_smoke_metrics.py(mock 대상 갱신) 동시 수정. Runtime Proof: 타겟 테스트 13/13 passed, 전체 suite 100 passed / 4 failed(pre-existing, test_dm_close.py 무관) / 3 xfailed — 260703 baseline과 정확히 일치. BOM 확인 — airtable_integrity.py / test_smoke_metrics.py 2개 파일 BOM 없음 확인(repository_interface.py / airtable_repository.py는 BOM 미검증). commit f6194ac, push 436bdf7..f6194ac 완료. |
| di_canary3_kpi_collector_260705 | ERR/FP 없음. repository_interface.py/airtable_repository.py 신규 메서드 2개(fetch_all_instagram_posts/fetch_all_lead_interactions) 추가, kpi_collector.py get_table 직접호출 2곳 Repository 치환, test_smoke_metrics.py 신규 테스트 4건 추가. 타겟 4건 PASS + 전체 104 passed·4 failed(pre-existing)·3 xfailed(baseline 일치). BOM 4개 파일 전부 확인. HOLD 신규: offset 페이지네이션 전체 미구현. |
| quality_gate_relevance_filter_canary_260706 | ERR-049/FP-037/INC-027 working tree 문서화 완료, commit 전. `quality_gate.py`에 relevance filter canary 편집(5번째 규칙) 추가 시 dry-run 검증을 영문 `caption` 필드 20건 기준으로 수행해 20/20 MATCH를 확인했으나, 실제 runtime 입력 필드(`title`, 한국어)와 불일치 — 영어-only 키워드가 한국어 title에 매칭 안 되어 D001/D002 `fetch=10 ready=0`(100% 차단) 발생. `git checkout HEAD -- modules\crawlers\quality_gate.py`로 원본 4규칙 rollback, launcher/main.py PID 지정 재시작으로 반영 확인. 재설계 미착수 — 한국어+영어 이중언어 키워드 기준 dry-run 후 재설계 필요. |
| quality_gate_relevance_filter_redesign_260707 | ERR-049/FP-037/INC-027(260706 canary 실패) 이후 재설계. 정책: category_code='Healthy'→무조건 READY, category_code='BEAUTY'→한국어+영어 COSMETIC_KEYWORDS/IRRELEVANT_HINTS 매칭(미매칭 시 기본 FILTERED). 30건 dry-run 기준 사용자 승인 라벨과 대체로 일치. 단, 상품유형 키워드가 없는 BEAUTY title edge case 1건은 기본 FILTERED 정책상 known limitation으로 승인됨. Canary 편집 후 launcher 재시작 중 중복 이슈 발생/정리 완료. Runtime Proof: D001(BEAUTY) fetch=10 ready=2, D002(Healthy) fetch=10 ready=10 — 정책대로 정상 동작 확인. DEFER: 키워드 보강(팩/패치/시트/수분/진정/미백/주름/피부 등), UNKNOWN 3단계 상태 도입 검토(READY/FILTERED만 저장하는 현 구조 제약), FILTERED 로그 별도 저장. commit/push 별도 승인 필요. |
| launcher_duplicate_instance_cleanup_260706 | ERR-048/FP-036/INC-026 등록. 세션 중 수동 반복 기동으로 launcher/main.py 5세대(10프로세스) 동시 생존 발견 → 8개 `Stop-Process -Force` 정리 + 단일 인스턴스(PID 33148/6140) 재기동, app.log상 스케줄러 1세트 정상 등록 확인. PID 20448/5284(전날 기동, Access denied)와 `:5000` 유령 LISTENING PID 32944(프로세스 열거 도구에 미포착)는 비관리자 권한으로 종료/식별 불가하여 미해결 — PARTIAL. |
| watchdog_task_wrapper_260708 | wrapper 경유 Task 트리거 시 direct(60초 내 사망)보다 오래 생존(1h46m, 정상 heartbeat 지속) — 이중 감시 정리 위해 의도적 종료(크래시 아님). 재부팅 자동 트리거 실증 완료(260709) — Task Action 발동 확인되나 wrapper 4분 24초 만에 silent death, 근본원인 UNKNOWN(ERR-047 Note 2 / ERR-050 Note 3 / INC-025 Note 교차참조) |
| inc028_1st_shutdown_root_cause_confirmed | INC-028 Note 3 / ERR-047 Note 5 — User32 Id=1074(20:09:52, StartMenuExperienceHost.exe 명의 종료 개시) → Kernel-Power/Kernel-General 종료 시퀀스(20:10:53 확정) raw 확인. Modern Standby(Id=506/507 공백 2시간1분 확인)·Windows Update·명시적 로그오프 전부 배제. 사람의 조작 가능성은 Hypothesis(확정 아님)로 남김. |
| heartbeat_wake_to_run_applied | ERR-053/FP-040 — `SNS_HeartbeatMonitor_Independent` Task `WakeToRun=False→True` 변경 적용(Settings 나머지 필드 불변 확인). 실제 Modern Standby 구간에서 heartbeat_monitor.log가 이어지는지 실증은 미완료 — PARTIAL 판정. |
| pending_a_session0_adspower_verified | `docs/PENDING_INVESTIGATIONS.md` PENDING-A — 진단 Task `SNS_DIAG_AdsPowerSession0Test`(LogonType S4U)로 AdsPower Local API 호출, `code=0`+`debug_port` 확인(SUCCESS). Task는 결과 확인 직후 Unregister로 제거, 재조회로 목록에서 사라짐 확인. |
| testabd_diagnostic_tasks_disabled | ERR-051 Note(260710) — `SNS_WatchdogAB_TestA/TestB/TestD` 3개 전부 `Disable-ScheduledTask`로 State: Ready→Disabled 확인(관리자 권한 재시도로 성공). 삭제 아님, 증거 보전 목적 유지. |
| watchdog_wakeup_applied_260710 | ERR-054/FP-040 — `SNS_Watchdog_AutoStart` Task `WakeToRun=False→True` 변경 적용(1차 비관리자 시도 Access denied 실패 → 2차 관리자 권한 성공). XML diff(WakeToRun 라인 1개만 변경)·taskinfo diff(LastRunTime/LastTaskResult 완전 동일)로 다른 필드 리셋 없음과 예약 인스턴스 영향 없음 실증 — PASS(설정 적용 + 무결성 확인 완료 기준). `heartbeat_wake_to_run_applied`와 달리 이 Task는 애초에 반복 트리거 절전 재현 검증 대상이 아니므로(로그온 1회성 트리거 구조), 실제 Modern Standby 상황에서의 효과 검증은 범위 외 — 절전 실증 완료로 과다 해석 금지. |
| backup_15_verified_260711 | ERR-055 — backup(14)(9.14MB, 크기 이상) 재검증 후 launcher/dashboard/n8n 전부 정지 → backup(15) 재생성(174,715KB, backup(13) 172MB와 동일 정상범위) + sha256 해시 생성 확인 — PASS. backup(14).zip은 삭제하지 않고 보존(증거 목적, 삭제 여부 별도 판단). |
| nssm_dual_watchdog_resolved_260711 | ERR-057/FP-042/INC-030 — NSSM 서비스(`SNS_Watchdog`)와 구 Task(`SNS_Watchdog_AutoStart`)가 watchdog.ps1을 이중 실행 중임을 프로세스 부모-자식 체인(PID 13008 vs 27664→28548)으로 확인. 관리자 권한으로 `Disable-ScheduledTask` 실행 → `schtasks /V`로 `Scheduled Task State: Disabled` 확인, 이미 떠 있던 구버전 PID 27664/28548을 `Stop-Process -Force`로 종료 → 재조회로 소멸 확인. NSSM 서비스(PID 13008) 단독 운영 전환, Flask(:5000)/Streamlit(:8501)/ngrok(:4040) 전부 영향 없이 LISTENING 유지 — PASS. |
| nssm_crash_restart_verified_260711 | PENDING-A 크래시 재시작 실증 — 관리자 권한으로 NSSM 관리 watchdog.ps1(PID 13008)을 `Stop-Process -Force`로 강제 종료(11:54:58) → NSSM이 `AppRestartDelay=60000ms` 설정대로 자동 재기동(`watchdog.log` 새 시작 배너 11:56:35 확인, 약 97초 후) → 재기동된 watchdog.ps1이 Streamlit까지 자체 정상화(PID 18048→31652), Flask `/health` HTTP 200 확인. 수동 개입 없이 완전 자동 복구 — PASS. 재부팅 실증은 별도 트랙으로 미실시. |
| nssm_reboot_proof_260711 | PENDING-A 재부팅 실증(잔여 트랙) — 사용자가 실제 재부팅 진행(12:09) 후 `Get-Service SNS_Watchdog` → `Running/Automatic` 자동 기동 확인, `watchdog.log`에 `===== watchdog 시작 =====` 배너가 이번엔 **1번만**(12:08:11) 기록됨(오늘 첫 재부팅 시 09:07:02/09:07:58 2번 기록됐던 것과 대비) — 구 Task 비활성화가 재부팅 전반에 걸쳐 유지됨을 실증. PASS로 PENDING-A(NSSM 서비스 전환) 완전 종결. |
| ngrok_localsystem_fix_260711 | ERR-058/FP-043/INC-031 — NSSM(LocalSystem) 전환으로 드러난 ngrok 이중 실패(MSIX 실행 불가 + authtoken 프로필 분리) 해결. `watchdog.ps1` `$NGROK_EXE` 포터블 경로 도입 + 사용자가 관리자 권한으로 authtoken(`ngrok.yml`)을 LocalSystem 프로필에 복사 → `Restart-Service` 후 12:35:48 `[RECOVER] Ngrok 복구` 확인, `/api/tunnels` 조회로 `public_url=https://danuta-overdramatic-whirly.ngrok-free.dev` 정상 응답 — PASS. |
| training_review_grid_50batch_260712 | ERR-059/INC-032 — 실제 50건 배치 저장이 GET 재검증 오탐으로 "실패" 표시됨. 47/50건 직접 재조회로 42 BLOCK+5 PASS 정확 저장 확인(3건 UNKNOWN). 오류 이후 브라우저 새로고침으로 원본 선택 세션 유실 — Airtable 현재값 역산은 자기 자신과 비교하는 무의미한 검증이라 판단해 완전 재검증 불가. 확보 증거로 **조건부 종결**, 신규 20건 배치부터 수정된 파이프라인으로 재개. |
| training_review_verification_error_fix_260712 | ERR-059/FP-044 — `review_batch_committer.py`에 `VerificationError`(status_code/error_type 보존) 신설, `mismatched_ids`(진짜 값 불일치)와 분리. `repository_interface.py`/`airtable_repository.py`에 status_code/Retry-After/original_error_type 전달 추가. 429는 Retry-After 기반, 5xx·타임아웃은 지수 백오프(최대 3회) 재시도, 403·404·기타는 즉시 처리(재시도 없음). `actual is None`(404 계약)을 `_verify_one()` 공통 헬퍼로 3개 함수(commit/undo/verify_only) 모두 동일하게 verification_errors로 분리(1차 수정 누락분, Codex 재검토로 발견 후 수정). `verify_only(repo, expected)` 신규(PATCH 없음). UI: verification_errors 시 확정 버튼 disabled + 새 배치 시 자동 해제. FakeRepo/mock 테스트 총 191 passed(4 failed는 기존 무관 `test_dm_close.py`) — PASS. |
| nssm_service_rebuild_260712 | ERR-060/FP-045/INC-033 — NSSM 서비스(`SNS_Watchdog`) 본체가 260711 23:08:47 예기치 않게 종료(원인 UNKNOWN), 등록된 `nssm.exe` 실행파일도 디스크에서 소실 확인. 고아 프로세스(3세대) 정리 → `choco install nssm -y --force`로 실행파일 복구했으나 서비스 등록 자체가 손상되어(`nssm get` 조회조차 실패) 재시작 불가 → `nssm remove`+`nssm install`로 서비스 완전 재생성(Application/AppParameters/AppExit/AppRestartDelay 동일 설정 복원) + `sc.exe failure`로 서비스 본체 크래시 복구 옵션 신규 추가 → `Get-Service` Running/Automatic, `sc.exe qfailure` SUCCESS, watchdog.log 새 시작 배너, Flask/Streamlit/ngrok 동일 PID 무중단 유지 확인 — PASS. 원인 규명은 git/chocolatey.log/Defender 탐지 로그 전부 조회했으나 인과관계 있는 흔적 없어 UNKNOWN으로 기록. |
| gate_c_price_safety_260713 | ERR-061/FP-046/INC-034 — `PRICE_AUTO_REPLY_ENABLED` 플래그(기본 `false`) 도입, 상품 미확정 시 가격 대신 상품확인 요청으로 대체, 발송실패·예외 시 `bridge_status` 오갱신·팔로업 오예약 방지, Telegram PII마스킹(신규 `send_telegram_price_pending()` 알림에만 적용 — 기존 `dm_receiver.send_telegram()` 노출은 미해결, P0-1 대상), `(sender_igsid, 정규화된 문의문)` 키 + `threading.Lock` 기반 원자적 임시 중복방지(Airtable 스키마 변경 없음, 3분 TTL, 실패·예외 시 선점 해제). **Claude 로컬 실행: `pytest tests/test_dm_rules.py` 30 passed**(신규 8건: 팔로업오발송방지/발송실패상태보호/Telegram마스킹/dedup 동일문의차단·다른문의허용·실패시해제·예외시해제·동시성 Lock). **Codex는 정적 코드감사로 PASS(테스트 직접 실행 안 함), 4라운드 재검토 거침.** 변경 3파일(`modules/dm/dm_auto_reply.py`/`.env.example`/`tests/test_dm_rules.py`), 432 insertions(+) / 24 deletions(-). **코드 구현·테스트 완료, 커밋 `c1c90b2`.** 260714 10:18 launcher 재시작(watchdog 자동복구 경유) + 10:24:41 통제된 Canary(로컬 웹훅 시뮬레이션)로 **가격 자동발송 차단 PASS 확정**(앱 로그 대조로 팔로업 오예약·bridge_status 오갱신 없음도 확인). **단 Canary가 가짜 IGSID였던 관계로 실제 IG 안내문 발송 성공·신규 `send_telegram_price_pending()` 마스킹은 E2E PARTIAL(미확인)** — 기존 `dm_receiver.send_telegram()` PII 노출(P0-1)은 이번 범위 밖, 계속 OPEN. |
| comment_pipeline_airtable_write_260714 | ERR-062/FP-047/INC-035 — Gate E-A 쓰기 Canary 검증 중 발견·실증: `comment_poller.py` 폴링(11:08)이 테스트 댓글 2건("price plz"/"dm")을 정상 감지했으나, `comment_auto_reply.py`의 `_record_comment()`가 `Lead_Interactions.conversation_channel`에 `instagram_comment` 선택지가 없어 Airtable 기록에 매번 실패(`INVALID_MULTIPLE_CHOICE_OPTIONS`). **최초 판단("토큰 권한 부족")은 오판으로 정정** — 같은 토큰에 `typecast:true`만 추가하면 정상 처리됨을 실증, 코드가 `typecast`를 안 쓰는 것은 오타 방지용 안전정책일 수 있어 버그로 확정하지 않음(코드 변경 없음). **Fix: Airtable에 `instagram_comment` 선택지 수동 추가(260714) + 저장 Canary 1건 PASS로 ERR-062/INC-035는 RESOLVED.** 다만 예외를 삼키는 함수+무조건 캐시하는 호출부 조합(FP-047)은 다른 원인의 저장 실패에도 동일하게 재발 가능 — **FP-047은 계속 OPEN.** `docs/design/DM_RELAY_COMMERCE_RFC.md` "기존 코드 결함(8건)" 1번 항목의 최초 실증. |
| gate_g_comment_private_reply_260714 | 댓글 자동응답을 공개 답글(`/{comment_id}/replies`)에서 **Private Reply**(`/{page-id}/messages`, `recipient.comment_id`+`message.text`)로 전면 전환 — 손님 댓글을 공개 노출 없이 DM으로 직접 유도하는 구조(회장 지시: "목적은 DM에서 상담"). 신규 `modules/comment/comment_safety_guard.py`(캠페인 게시물 allowlist `configs/comment_campaign_posts.json`, 사용자별 24시간 쿨다운, 일일 발송 30건 예산, 3회 연속실패 circuit breaker, 상태파일 손상 시 fail-closed, `os.replace` 원자적 쓰기, `threading.Lock` 기반 REPLY_LOCK). `comment_auto_reply.py`: `PRICE_REPLY_TEMPLATES` 4종 랜덤 로테이션+개인화+옵트아웃 안내(문구 다양화 자체는 UX 개선용이며 "메타 필수요건"이 아님을 명시 — 최초 과신 표현 정정). `comment_poller.py`/`dm_receiver.py`(웹훅 경로) 둘 다 `from.id`(안정적 IGSID)를 `commenter_id`로 추출해 쿨다운 키로 사용(username 변경으로 인한 쿨다운 우회 방지). **Codex 4라운드 리뷰**: 1차(엔드포인트가 구 Facebook Page 댓글용 `/{comment-id}/private_replies`로 잘못 구현됨/fail-open 안전장치/24시간창 설명오류) → 수정. 2차(엔드포인트 ID가 `PAGE_ID`가 아니라 `INSTA_IG_USER_ID`여야 한다는 재지적) → Claude가 `developers.facebook.com/docs/messenger-platform/instagram/features/private-replies/`(Facebook Login for Business 제품 문서, 우리 인증모델과 일치) 직접 fetch로 `PAGE_ID`가 맞음을 반박·재확인, 이 라운드에서 웹훅 commenter_id 누락/동시성(REPLY_LOCK)/문구가 "이미 DM인데 DM으로 오라"는 논리오류였던 점/message_id 로깅누락은 실제 결함으로 인정해 수정. 3차(Codex는 자신이 인용한 Meta 공식 Postman 문서는 정상 확인했고, 429로 재확인 못한 쪽은 Claude가 제시한 구형 문서였음 — 최초 기록 시 이 경위를 반대로 적었다가 회장 재검토로 정정, 260714. 그럼에도 "일반 DM 성공이 recipient.comment_id 성공의 증거는 아니다"라는 논리로 ID 미확정 보류) → **통제된 실제 Canary로 해결**: 동의된 계정(tgbtgbnate)이 실제 게시물에 댓글("관심 있어요", `comment_id=17916708546421368`, `from.id=4420182554922853`) 남김 → 서버에서 `reply_privately_to_comment()`를 자동화 파이프라인 우회하고 단 1회 직접 호출(`.env`는 `false` 유지, 캠페인 allowlist도 미변경 — 지속 자동화는 안 켬) → `resp.ok=True`(HTTP 2xx, Meta가 `PAGE_ID`+`recipient.comment_id` 계약을 실제로 수락) 확인 + **회장이 tgbtgbnate 계정 DM함에서 실제 메시지 도착을 육안으로 확인** — `PAGE_ID` 계약이 최종 실증 확정됨. (message_id는 독립 스크립트 실행이라 로거가 app.log에 연결 안 돼 캡처 못함 — 실제 도착 확인이 더 강한 증거라 문제 없음.) **로컬 테스트: `pytest tests/test_comment_safety_guard.py tests/test_comment_auto_reply.py tests/test_meta_graph_version.py` 44 passed**(신규 fail-closed 5건, 동시성 실스레드 검증 1건, 문구다양화 4건 포함), 전체 스위트 270 passed(무관 기존 실패 4건 `test_dm_close.py` 제외). **상태: 코드+테스트+라이브 엔드포인트 계약 검증 전부 PASS. 지속 자동화(`.env=false`·캠페인 allowlist 빈 배열)는 영향 0이지만, 승인된 통제 Canary로 실제 DM 1건이 발송·수신 확인됨 — "운영 영향 0"으로 뭉뚱그리지 않고 명시. 아직 미커밋** — 커밋은 이 문서화와 별도 승인 대상. |
| gate_e_b_v25_migration_260714 | Graph API v19.0→v25.0 마이그레이션(DM·댓글 4파일 8곳) — 신규 `modules/common/meta_graph.py`(기본값 `v25.0`, `META_MESSAGING_GRAPH_API_VERSION` override, 버전형식·절대URL 검증)로 `messaging_graph_url()` 도입, `dm_auto_reply.py`/`dm_followup_scheduler.py`/`comment_poller.py`/`comment_auto_reply.py`의 URL 생성부만 교체(payload/헤더/timeout/업무로직 불변). 기존 v21.0 업로드·인터랙션 8곳(5파일)은 변경 없음 — grep으로 v19.0 0건, v21.0 8건(동일 5파일) 확인. **`pytest tests/test_meta_graph_version.py` 14 passed**(신규 URL빌더 10건 + 모듈연결 4건, `requests.get/post` 전부 monkeypatch — 실외부호출 없음). 코드·단위테스트 PASS 상태에서 커밋 `102c128`. **운영 반영(재시작): 회장이 관리자 권한으로 구PID(33008, 커밋 이전 기동)를 `Stop-Process`, watchdog 자동복구 경유 PID 48560(중간) → 이후 통제된 재시작으로 최종 PID 17780/NSSM 서비스 PID 2908(둘 다 CreationDate 12:40, 커밋 12:17 이후) 확인 — 최신 코드 실행 중 확정.** **라이브 Canary(실제 테스트 계정, 4경로 중 3경로 PASS):** ① `dm_auto_reply` — 실제 DM 단가문의 → `PRICE_AUTO_REPLY_ENABLED=false`로 상품확인 요청 대체 응답, `messaging_graph_url()` 경유 실제 발송 성공(msg_id 반환, 13:13:22~24) PASS. ② `dm_followup_scheduler` — 기존 대기 레코드(`LI-798F44CE`)는 불변, 전용 신규 Canary 레코드(`LI-CANARY-GATEEB-260714`/`recZ3ylf3frOZbmNc`, 동일 실계정 IGSID 재사용) 생성 후 `relay_scheduled_at`을 과거로 설정 → 5분 주기 스케줄러가 자연 픽업(13:05:29~33)해 실제 발송 성공(msg_id 반환) → Airtable `bridge_status` `auto_replied→followup1_sent` 전이 재조회로 확인 PASS. 검증 직후 회장 승인 하에 `bridge_status=closed`+`relay_scheduled_at` 공란 처리로 내일 `followup2` 오발송 방지(증거 로그·msg_id는 보존). ③ `comment_poller` — 실제 테스트 댓글("DM주세요") 폴링 사이클(13:15:29~47)에서 `GET {ig_user_id}/media`+`GET {media_id}/comments` 두 v25.0 호출로 정상 감지·Airtable 기록 완료 PASS. **④ `comment_auto_reply`(답글 `POST /{comment_id}/replies`, `me/accounts` 토큰조회 포함 2개 v25.0 호출부) — 라이브 미검증.** 트리거 조건(`COMMENT_AUTO_REPLY_ENABLED=true` + 단가 키워드 댓글) 충족 위해 `.env`를 일시 `true`로 수정했으나, 재시작 시 전체 실사용자 댓글에 공개 자동답글이 노출될 운영 위험을 회장이 재시작 직전 판단해 중단 — 프로세스는 재시작되지 않아 `true` 설정이 실제 반영된 적 없음(PID 17780은 재시작 없이 계속 기존 `false`로 동작), 이후 `.env`를 즉시 `false`로 원복 확인. 이 경로는 **`pytest tests/test_meta_graph_version.py`의 mock 단위테스트 PASS로만 검증된 상태이며 라이브 미검증으로 남음** — 별도 승인 없이 활성화 금지. **로그 한계 메모:** `meta_graph.py`/4개 호출부는 생성한 URL 문자열 자체를 로그에 남기지 않음(성공 시 `msg_id`만 기록) — v25.0 증거는 (a)8개 호출부 전부 `messaging_graph_url()` 경유(코드 확인) (b) helper 기본값 `v25.0`(코드 확인) (c) 위 3개 경로의 실제 발송/조회 성공(런타임 확인) 조합으로 구성. Meta 응답의 API-Version 헤더 별도 확인은 미실시. **종합: 3/4 경로 운영 반영 라이브 PASS, 1/4(`comment_auto_reply` 답글 POST)는 단위테스트 PASS·라이브 미검증. 문서화까지는 완료, 커밋·push는 별도 승인 대상.** |
| fp047_comment_idempotency_260715 | ERR-067/FP-047/INC-035 — 댓글 이벤트 idempotency(웹훅+폴러 이중진입 dedup, Airtable 기록 재시도, crash 복구) 구현. GPT/Codex 총 12라운드 교차검토(설계 8라운드 + 구현 후 코드 리뷰 4라운드). 신규 `modules/comment/comment_event_store.py`(fencing token 포함 원자적 claim, stale lease 자동 회수 내장), `modules/comment/comment_retry_dead_monitor.py`(retry_queue dead 태스크 Slack 알림 + event_store 동기화). 단일 진입점 `process_comment_event()`(`comment_auto_reply.py`) — `COMMENT_EVENT_STORE_MODE`(disabled/shadow/enforce) 킬스위치, `CommentProcessResult`(ACCEPTED/DUPLICATE_COMPLETED/RETRY_OWNED/IN_PROGRESS/LEGACY/REJECTED_NOT_READY) 구조화 반환값. Airtable `Lead_Interactions.source_event_id` 필드 신규 추가(API로, `tools/add_lead_interactions_source_event_field.py`) + 3-way 조회(FOUND/NOT_FOUND/LOOKUP_FAILED)로 재시도 시 중복 방지. `dm_receiver.py` 웹훅을 2단계 처리(댓글 전부 durable-accept 확정 후에만 DM 처리, 실패 시 503로 Meta 재전송 유도 — DM 쪽 신규 중복 방지)로 재구성. 구현 과정에서 발견·수정된 correctness 버그 9건은 ERR-067에 상세 기록(reclaim 미연결, fencing 반환값 무시, retry token 노후화, shadow 오염 등 — 전부 다단계 리뷰의 실제 코드 재현으로 확인 후 수정). **로컬 테스트: 신규 65개(동시성 10스레드 경쟁/fencing 위조token 거부/crash 재현/shadow 격리/webhook 2단계 처리 등) 전부 통과, 전체 회귀 345 total/338 passed/4 failed(무관 기존 `test_dm_close.py`)/3 xfailed.** **상태: 코드 구현 완료, GPT/Codex sign-off 완료. `COMMENT_EVENT_STORE_MODE=disabled`(기본값)로 커밋 — 기존 운영 동작 전혀 안 바뀜.** enforce 진입 전 필수 해결 항목(OPEN): 댓글 원문 평문 저장(ERR-066과 같은 클래스), Airtable 필드 존재 startup preflight 미구현. 마이그레이션 CLI 도구·완전한 dead-alert 원자적 상태머신은 fast-follow로 미구현. shadow/enforce 전환 및 실계정 Runtime Proof는 이 커밋과 별도 승인 대상. |
| package1_phase_a_allowlist_polling_260716 | ERR-069/FP-050/INC-038 — "최근 게시물 N개" 폴링 한도로 캠페인 댓글이 시스템에 아예 진입 못 하는 결함을 실사용자 테스트로 발견 후 근본 수정(Package 1 Phase A). 신규 `modules/comment/comment_poll_targets.py`(media별 `PENDING_BASELINE→ACTIVE→PAUSED` 상태머신, JSON↔DB 동기화), `modules/comment/comment_campaign_config.py`(캠페인 allowlist 공용 loader, guard/poll_targets 공유), `tools/comment_campaign_baseline_cli.py`(media별 수동 cutover — `--dry-run`/`--apply --expected-config-hash`/`--verify`/`--activate --acknowledge-runtime-proof`, 8개 검증 계약 포함). `comment_poller.py`를 `COMMENT_POLL_ALLOWLIST_MODE`(기본 legacy) 플래그로 `_poll_legacy()`(기존 "최근 N개", 무변경)/`_poll_allowlist()`(신규, 전체 페이지네이션)로 분리 — 기본값에서는 폴링 대상 선택이 기존 recent-N 방식으로 유지되나, 캠페인 설정/poll-target DB 이상 시 신규 안전 게이트가 fail-closed로 처리를 차단할 수 있어 "운영 동작이 전혀 안 바뀜"은 아님(260716 Codex 재검토로 정정). `comment_auto_reply.py`의 `process_comment_event()` 최상단에 `_blocked_by_allowlist_gating()` 게이트 추가(event-store 모드·mode 분기보다 먼저, event_store 행 생성 전에 검사) — 캠페인 media가 PENDING_BASELINE/JSON에서 막 제거됨/설정 손상 중 어느 하나여도 실처리를 차단. **GPT 전략자문 1라운드 + Codex 코드검수 9라운드**(설계 검토 아님, 구현 완료 후 실제 코드 재현 확인 기반 리뷰) — 라운드마다 실제 버그 발견: PENDING media 새 댓글이 SHADOW_SEEN 태그로 영구 고착돼 나중에 ACTIVE 전환 후에도 처리 못 하는 버그(재현 테스트로 확인 후 수정), legacy 모드가 전체 페이지네이션을 써서 배포만으로 과거 댓글 대량발송 위험이 재현되던 버그, disabled 모드가 게이트를 우회하던 버그, JSON에서 방금 제거된 ACTIVE media가 DB 동기화 전까지 통과되는 경쟁 구간, `--activate`가 "경고만"으로는 불충분해 하드블록 필요(allowlist+shadow+ACTIVE 조합이 다음 폴링 주기부터 실제 발송으로 이어짐을 재현 확인) 등. **로컬 테스트: 신규 87개(상태머신 전이/baseline CLI 8계약/게이팅 시나리오 재현 등) 전부 통과, 전체 회귀 424 total/416 passed/5 failed(무관 기존 `test_dm_close.py` 4건 + flaky `test_review_grid_ui.py` 1건, 반복 실행으로 flaky 정황 확인되었으나 원인조사 전이라 공식 UNCLASSIFIED 유지)/3 xfailed.** **상태: 코드 구현 완료, Codex 조건부 승인(체크포인트 커밋 PASS, 운영 전환 미승인). `COMMENT_POLL_ALLOWLIST_MODE=legacy`(기본값)로 커밋 예정 — 이 인시던트(INC-038)를 만든 방식이 그대로 유지된 상태.** enforce/allowlist 전환 전 필수(OPEN): 자동 Runtime Proof 시스템(launcher가 PID·boot_id·모드를 DB에 남기고 CLI가 교차검증, 지금은 `--acknowledge-runtime-proof` 수동 선언만 존재 — Phase C/D 과제로 명시 합의). `.env.example` 킬스위치 3종(`COMMENT_POLL_ALLOWLIST_MODE`/`COMMENT_POLL_MAX_PAGES`/`COMMENT_POLL_FAILURE_ALERT_THRESHOLD`) 등록은 이번 staged 범위에 이미 포함 완료. 가격 키워드 확대(회장 260715 별도 지시)는 이번 커밋에서 명시적으로 제외 — hunk 단위로 분리해 스테이징. |
| fp047_enforce_precondition_a_260716 | ERR-070/FP-051 — FP-047 enforce 전제조건 2개(A: 댓글 원문 평문 저장, B: Airtable startup preflight) 중 **A만 완료**. 댓글 원문이 로그(`app.log`)·Telegram·retry payload(`db/retry_queue.db`) 3곳에 평문으로 남던 문제(ERR-066과 같은 클래스)를 해소. 로그·Telegram은 공용 마스킹 유틸(신규 `modules/common/pii_mask.py` — DM 채널용 `_mask_igsid`/`_telegram_preview`를 도메인 중립 위치로 추출, ERR-070/FP-051 순환임포트 해결과 동시 진행) 재사용해 PII 정규식 마스킹+20자 미리보기로 통일(username은 공개 IG 핸들이라 마스킹 제외). retry payload는 Fernet 대칭키 암호화(`COMMENT_PAYLOAD_ENC_KEY`, `requirements.txt`에 `cryptography` 명시 등록)로 전환 — `enc_version` 저장 및 재처리 시 엄격 검증(버전 불일치·`text`/`text_enc` 동시 존재·둘 다 없음은 전부 `ValueError`로 fail-closed, 구형 평문 fallback은 배포 시점 `db/retry_queue.db` 실측 0건 확인 후 완전 제거). `register_retry_handlers()`에서 launcher 시작 시 키 왕복 검증하되, 실패해도 launcher 전체(FB크롤링/IG업로드/DM 등)는 막지 않고 **enforce 모드의 댓글 처리만** 거부(회장 결정: blast radius를 댓글 도메인 안으로 한정). **Codex 2라운드 리뷰**(1차: `cryptography` 미등록 의존성/`enc_version` 미검증/`.env.example` 오기술(disabled·shadow도 영향받는 것처럼 썼으나 실제로는 `claim_token=None` 레거시 경로가 retry_queue 자체를 안 씀)/미사용 `InvalidToken` import 지적 → 전부 반영, 2라운드: 조건 없이 PASS). **로컬 테스트: 신규 18개**(`test_comment_payload_encryption.py` 16 + `test_comment_auto_reply.py` 마스킹 검증 2), 기존 `test_comment_airtable_idempotency.py` 3개가 구형 payload 형식이라 이번 fail-closed 강화로 깨진 것을 발견해 `text_enc`/`enc_version` 형식으로 갱신. **전체 회귀 396 passed / 4 failed(무관 기존 `test_dm_close.py`) / 3 xfailed.** **상태: A 완료, `COMMENT_EVENT_STORE_MODE`/`COMMENT_POLL_ALLOWLIST_MODE` 등 운영 모드 무변화(A-2 암호화 경로는 enforce 모드에서 Airtable 1차 쓰기 실패 시에만 실행되며, 현재 운영은 여전히 shadow). B(Airtable `Lead_Interactions.source_event_id` 필드 존재 startup preflight)는 미착수 — FP-047 enforce 전제조건 전체 완료 아님, push도 미실행.** |
| fp047_enforce_precondition_b_260716 | FP-047 enforce 전제조건 **B 완료**(A는 이미 완료, 이번 세션에서 전제조건 A+B 둘 다 마감). `repository_interface.py`에 `verify_field_exists(table, field_name) -> bool` 추상 메서드 추가, `airtable_repository.py`가 Metadata API(`/v0/meta/bases/{base}/tables`)로 구현(기존 `tools/add_lead_interactions_source_event_field.py`와 동일 엔드포인트 재사용, 신규 의존성 없음). `comment_auto_reply.py`에 `_verify_airtable_preflight()` 신규(`_verify_payload_cipher()`(A-2)와 완전히 동일한 패턴) — launcher 시작 시 `Lead_Interactions.source_event_id` 필드 존재 확인, 실패해도 launcher 전체는 막지 않고 **enforce 모드 댓글 처리만** 거부(A-2와 동일 blast-radius 원칙). **Codex 리뷰 대상 지적 3건**(verify_field_exists가 기존 `_raise()`/`RepositoryUnavailableError` 패턴 준수 여부/preflight 실패 시 comment-only 거부 원칙 일관 적용 여부/startup 1회 검사의 런타임 중 필드삭제 미감지 한계) — Codex 판정: 코드 자체는 PASS, 한계 ③은 "향후 주기적 health check 후보"로 backlog 전환(이번 범위 아님). **로컬 테스트: 신규 11개**(`test_airtable_repository_field_preflight.py` 5 + `test_comment_airtable_preflight.py` 6) 전부 통과. **상태: FP-047 enforce 전제조건 A+B 모두 완료. `COMMENT_EVENT_STORE_MODE`/`COMMENT_POLL_ALLOWLIST_MODE` 등 운영 모드 전환은 미실행(별도 승인 대상), push도 미실행.** (B 구현 중 발견된 무관 테스트 실패 2건의 원인 규명·수정은 별도 항목 `test_isolation_cooldown_hours_260716` 참조 — 별도 커밋으로 분리.) |
| test_isolation_cooldown_hours_260716 | ERR-071/FP-052 — B의 신규 테스트 파일 2개 추가로 pytest 전체 수집 순서가 바뀌며, B와 무관한 기존 테스트 2건(`test_reply_lock_serializes_concurrent_calls_prevents_double_send`/`test_mark_user_replied_recovers_from_corrupted_state`)이 전체 회귀에서 실패. 최초엔 "단독 실행 시 통과"를 근거로 무관 판단했으나 Codex 재검토("단독 통과는 무관 증거 아님")로 재조사, **실제 원인 규명 완료**: `comment_safety_guard.COOLDOWN_HOURS`가 모듈 import 시점에 실제 `.env`(`COMMENT_REPLY_COOLDOWN_HOURS=0`, 260715 회장 지시)로 고정되는 구조라 두 테스트가 이 값에 암묵 의존하고 있었음. `tests/test_comment_safety_guard.py`(autouse fixture)/`tests/test_comment_auto_reply.py`(REPLY_LOCK 테스트)에 `monkeypatch.setattr(guard, "COOLDOWN_HOURS", 24)` 명시 추가로 해결. **반복 실행 2회 모두 `tests/ -k "comment or repository or airtable"` 219 passed/0 failed로 재현**(우연한 재통과 아님 확인) 후 전체 프로젝트 회귀도 원래 베이스라인(407 passed/4 failed — `test_dm_close.py`만 무관/3 xfailed)으로 정확히 복귀 확인. **B의 실제 프로덕션 코드는 무관 — B의 테스트 파일 추가가 기존 잠재 결함을 표면화시킨 방아쇠였을 뿐.** |
| watchdog_utf8_bom_recovery_260721 | ERR-072/FP-053/INC-039 — `watchdog.ps1` UTF-8 BOM `EF BB BF` 확인, Windows PowerShell 5.1 `Parser.ParseFile()` 오류 0건, 신규 `tests/test_watchdog_encoding.py` 2 passed. NSSM 자동 재시도 후 `SNS_Watchdog=Running/Automatic`, 새 시작 배너 11:32:16, Streamlit 8501/Flask 5000/ngrok 4040 HTTP 200 확인 — **PASS**. 실제 OS 재부팅 재현은 미실시(PENDING). |
| adspower_dependency_recovery_260721 | ERR-073/FP-054/INC-040 — 실패 시 AdsPower 프로세스·50325 없음과 FB 4개 그룹 `WinError 10061`/0건 확인. AdsPower UI 실행 후 버전 창 확인 및 50325 LISTENING 복구 — **서비스 의존 포트 복구 PASS**. 다음 FB 크롤링 E2E와 재부팅 후 자동기동은 미검증(PARTIAL). |
| n8n_watchdog_disabled_260721 | ERR-065/FP-049/INC-037 — LocalSystem에는 `n8n.cmd` 없음, admin 프로필에만 존재하고 `npx n8n start`가 대화형 설치 질문에 진입하는 근본원인 확인. `N8N_WATCHDOG_ENABLED=false` 기본값 추가, PowerShell Parser 오류 0, `tests/test_watchdog_encoding.py` 3 passed. 서비스 재시작 후 12:16:54 비활성화 로그, 마지막 실패 12:16:38 뒤 재시도 0건 — **임시 완화 PASS**. n8n 기능은 미기동(PENDING). |
| adspower_startup_shortcut_fixed_260721 | ERR-073/FP-054/INC-040 — 공용 시작프로그램 바로가기 대상 `AdsPower.exe`(없음) → `AdsPower Global.exe`(존재) 수정, `TargetExists=True`, 50325 LISTENING. 다음 예약 FB 크롤링 4개 그룹 연결 성공·총 1건 처리 — **E2E PASS**. |
| adspower_reboot_autostart_verified_260721 | ERR-073/FP-054/INC-040 — 회장 승인 후 실제 `Restart-Computer -Force` 재부팅 실증. `watchdog.log` 원본: 13:14:41 FATAL 종료 → 13:15:13 SNS_Watchdog 자동 재기동 → 13:15:18~37 Streamlit/ngrok/launcher 자동 복구 → 13:17:32~40 AdsPower Global 8개 프로세스 자동 실행. 재부팅 후 50325/5000/8501/4040 전부 LISTENING 재확인 — **재부팅 자동기동 PENDING 항목 PASS로 종결**. |
| engagement_stale_media_cleanup_260721 | ERR-039/FP-055/INC-021 — `posted + ig_media_id` 291개 중 접근 불가 6개를 계정·토큰 정상 상태에서 개별 100/33으로 확정. 승인 후 6개 ID를 조건부 공란 처리하고 6/6 `null` 재조회. 신규 게시물 4개 포함 현재 대상 289/289 Graph API 접근 가능 — **PASS**. |
| training_review_queue_stalled_260721 | ERR-074/FP-056/INC-041 — 학습용 크롤(`run_for_training_photos`)이 `launcher/main.py`/`core/run_engine.py` 어디에도 미등록 확인(grep "training" 0건), 유일한 호출부는 수동 러너 `tools/_run_training_photo_crawl.py`(260713 커밋 `17dae25` 메시지에 "반복 실행용이라 미커밋"으로 명시). 로그 기준 마지막 수집 2026-07-13 00:32:17, 이후 260721 21:19까지 8일간 `[Training]` 로그 0건 확인(동일 기간 `[FB Crawler]`는 정상 기록 — 시스템 장애 아님). Training_Review_Queue 전체 299/PASS 56/BLOCK 243/PENDING 0으로 8일 미수집과 정합. **read-only 조사 완료(PASS), 해결 방안(수동 A vs 자동화 B)은 회장 결정 대기(OPEN)**. |
| instagram_provider_routing_design_260725 | 9단계(2계정 재현 Test) 구조적 블로커 해소 — `Account_Registry`(`IDN-000036`/`aijomoojin`)가 회사계정(`yuna18253`)과 다른 Meta API 계열(Instagram API with Instagram Login, `graph.instagram.com`)을 쓴다는 사실을 read-only GET(`account_type=MEDIA_CREATOR` 확인)으로 실측, 계정별 Provider 분기 최소변경을 설계. Codex 읽기전용 리뷰 2라운드(`account_code_ref` 필드타입 오판 1건 직접 재검증으로 정정 — 실제 `multilineText`, `list[str]` 아님) + GPT 아키텍처 감사 2라운드(`ig_user_id` Airtable/.env 이중기준·`credential_key` 형식검증 확정) 거쳐 `DESIGN_READY`→회장 구현승인. 구현: `Account_Registry`에 `api_provider`/`credential_key` 필드 신규(값 미포함, State DB Only 원칙 준수) + `IDN-000036` 값 입력, 신규 `modules/common/credential_resolver.py`, `repository_interface.py`(`account_code_ref`/`PublishAccount`)+`airtable_repository.py`(`get_publish_account()`)+`launcher/main.py`(`PROVIDER_CONFIG`, claim 전 계정조회·ig_user_id 교차검증·Provider allowlist, `INSTAGRAM_PROVIDER_ROUTING_ENABLED=false` 킬스위치) 변경. 신규 테스트 25개(`test_credential_resolver.py` 13 + `test_provider_routing.py` 12, 미지원Provider·중복계정·ig_user_id불일치·혼합배치 크로스오염 등 리뷰 필수조건 전부 커버) 전부 PASS, 전체 회귀 `557 passed/5 failed(기존 무관 baseline: test_dm_close.py 4 + test_review_grid_ui.py flaky 1)/3 xfailed` — 신규 회귀 0건. **Flag 기본값 false로 커밋, 실제 게시 동작 무변경. git commit·Runtime 배포·Flag 활성화·Canary는 이번 범위 밖(각각 별도 승인 대상).** |
| instagram_provider_routing_canary_260725 | 위 설계의 실제 배포+Canary 게시까지 완료 — 9단계(2계정 재현 Test) **최종 완료**. ①`/media_publish` timeout 중복게시 방지(Codex STOP ITEM) 추가 구현: `publish_single()` Phase A(`/media`)/Phase B(`/media_publish`) 분리, creation_id 확보 후 새 컨테이너 생성 금지, `ConnectTimeout`만 제한재시도·나머지 모호한 실패는 `outcome_unknown`으로 즉시격리(`uploading` 유지, `mark_post_result()` 미호출, Slack 즉시알림) — Codex 읽기전용 리뷰 3라운드(1차 설계지적→구현→2차 버그 2건 발견(빈id값 오판/로그라벨 오표기)→수정→3차 `STOP_ITEM_CODE_GATE: PASS`). 신규 테스트 15개(`test_publish_outcome_unknown.py`) PASS, 전체회귀 `572 passed/5 failed(동일 baseline)/3 xfailed`. commit `a33b506`. ②Runtime 배포: `SNS_Watchdog` 서비스 재시작 2회(회장이 관리자 권한으로 직접 실행, 이 세션은 권한 없어 실행 불가 확인) — 재시작마다 포트/프로세스/로그 정상 확인, 신규 회귀 0. ③Flag 활성화: `.env`에 `INSTAGRAM_PROVIDER_ROUTING_ENABLED=true` 추가 후 재시작. ④`aijomoojin` 실제 Canary: 8단계와 동일 캡션 유지, 이미지는 최초 imgbb URL이 `graph.instagram.com`에서 다운로드 거부(HTTP 400, code 9004)돼 Wikimedia 호스팅으로 교체 후 재시도 — 자동 잡의 1차 시도는 Phase B에서 HTTP 400(컨테이너 처리중 추정, ERR-076 신규 기록)으로 `failed` 마킹됐으나, 수동 재시도(같은 creation_id)로 실제 발행 성공 확인. **최종 증거**: `ig_media_id=18110242561955523`, `permalink=https://www.instagram.com/p/DbMth5Skgy_/`, Graph API GET 2회 + 공개 브라우저(비로그인) 접속 + 계정 소유자 본인 화면 확인 = 4중 검증. Airtable `post_status=posted`로 수동 정정 완료(레코드 `recHTfHrFPQh79XGy`). **미해결 후속과제(OPEN, ERR-076/FP-058 기록): HTTP 4xx "재시도 금지" 규칙에 실측 반례(컨테이너 처리중 400→재시도시 200) 존재, 현재는 fail-closed로 안전하나 자동복구는 안 됨.** imgbb 호스팅이 `graph.instagram.com` 계열 전체와 구조적으로 안 맞을 가능성도 후속 조사 필요. |
| token_rotation_yuna18253_260725 | 7-C Token 교체(GPT 260725 확정 1순위 과제, [[project_workflow_architecture_priority_260723]]) — 로그 노출 재조사(read-only)로 메모리의 "596건, 260723 감사" 주장을 실측 재현: `INSTA_ACCESS_TOKEN`(yuna18253) 243건(`logs/` 6개 파일: `app.log.1` 25 / `app.log.5` 208 / `AI_chat/*.txt` 4개 파일 합 10) 확인, `AI_INSTA_ACCESS_TOKEN`(aijomoojin) 0건 — 노출된 yuna18253 토큰만 교체 대상으로 확정(596과 243 불일치 사유는 원본 감사 방법론 문서 부재로 UNKNOWN 기록). 1차 재발급 시도가 잘못된 Meta Use Case(Instagram API with Instagram Login)로 이뤄져 IGAA 토큰 저장 → `graph.facebook.com` `OAuthException 190`(ERR-077/INC-043) → Graph API Explorer의 Page Access Token 경로로 정정 재발급(EAA)해 해소(FP-059 신규 기록). **최종 검증**: `GET https://graph.facebook.com/v21.0/{ig_user_id}?fields=id,username` → HTTP 200, `id=17841476202821375`(기존과 일치)/`username=yuna18253` — **PASS**. 구 IGAA 토큰은 `.env`에 `INSTA_FBCRAWING_ACCESS_TOKEN`(비운영 백업, 코드 미참조)으로 보존. 구 EAA 토큰(노출분)은 재발급으로 자연 무효화됐을 것으로 추정되나 Meta 콘솔에서 명시적 revoke 확인은 미실시(UNKNOWN). 로그에 남은 243건 원문은 삭제하지 않음(영구삭제는 별도 승인 대상). `SNS_Watchdog` 재시작 2회 모두 회장이 관리자 권한으로 직접 실행. |
